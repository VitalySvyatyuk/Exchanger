# Vitest lists many optional peer dependencies (browser runners, coverage
# providers); resolving them crashes older npm versions. Install without
# auto-installing peers, so local installs and `npm ci` in CI resolve the
# lockfile the same way.
legacy-peer-deps=true

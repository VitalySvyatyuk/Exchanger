# PostgreSQL connection string.
# Matches the database started by `docker compose up -d`.
DATABASE_URL="postgresql://exchanger:exchanger@localhost:5432/exchanger?schema=public"

# Admin user created by `npm run db:seed`.
# If ADMIN_PASSWORD is empty, the seed generates one and prints it once.
ADMIN_EMAIL="admin@exchanger.local"
ADMIN_PASSWORD=""
